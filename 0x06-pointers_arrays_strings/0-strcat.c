#include "main.h"
#include <stdio.h>

/**
 * *_strcat - a function that concanate a string
 * @dest : first string
 * @src : second string.
 * Return: always 0
 */

int main(void)
{
strcat(*dest, *src);
return (0);
}
